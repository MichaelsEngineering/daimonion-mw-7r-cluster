[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mwpack"
version = "0.0.0"
description = "MVP artifact maker: markdown in, attested deterministic bundle out"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
